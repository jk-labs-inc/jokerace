"use client";
import LayoutContests from "@layouts/LayoutContests";

const ContestsLayout = ({ children }: { children: React.ReactNode }) => {
  return <LayoutContests>{children}</LayoutContests>;
};

export default ContestsLayout;
